Create a node.js file that Select all records from the "customers" table, and delete the specified record

var mysql = require('mysql');
var con = mysql.createConnection({
host: "localhost",
user: "root",
password: "root",
database: "msc"
});
con.connect(function(err) {
if (err) throw err;
var sql = "DELETE FROM customers WHERE address = 'Pune'";
con.query(sql, function (err, result) {
if (err) throw err;
console.log("Number of records deleted: " +
result.affectedRows);
});
});
----------------------------------------------------------------------------------------------------------------------
Execution Steps –
1) Save the file and run the script using Node.js: node database_operations.js
2) The script will connect to your MySQL database, select all records from the "customers" table, and
then delete the specified record based on your condition.
3) The execution will output the selected records and the number of deleted records.
4) Finally, it will close the database connection.
Number of records deleted: 0
